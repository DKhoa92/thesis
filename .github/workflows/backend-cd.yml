name: Trigger Build And Deploy Backend

on:
  push:
    branches:
      - backend
    paths:
      - 'backend-nestjs/**'
  workflow_dispatch:

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello World"
